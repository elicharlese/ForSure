#!/usr/bin/env bash

set -euo pipefail

# Run linting and formatting
pnpm run lint:fix
pnpm run format
# Stage any auto-fixes so they are included in the commit
git add -A

# Temporarily skip tests and type-check in pre-commit
# TODO: Re-enable after coverage and TS errors are fixed
# pnpm run test:ci
# pnpm run type-check
