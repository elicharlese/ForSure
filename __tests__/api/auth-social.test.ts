/**
 * Social auth routes have been removed during the Prisma + JWT migration.
 * These tests are intentionally skipped until a new OAuth flow is reintroduced.
 */

describe.skip('Deprecated social auth routes', () => {
  it('placeholder - OAuth flow deprecated', () => {
    expect(true).toBe(true)
  })
})
